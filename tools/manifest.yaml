# Tool manifest — tools this talent can use.
#
# research-eval is engine-backed: the agentic loop in evaluator.py drives ~16
# internal tools (read_paper, search_in_files, run_command, python_eval,
# web_search, web_fetch, invoke_skill, submit_review, ...). From the talent's
# perspective that whole engine is exposed as one custom tool, declared below
# and documented in tools/research-eval/TOOL.md.

tools:
  - research-eval
